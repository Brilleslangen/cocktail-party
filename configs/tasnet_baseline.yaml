defaults:
  - model_arch: tasnet      # Architecture selection
  - dataset: static         # Dataset selection
  - training: default

  # Override architecture defaults | Null if not used
  - encoder:   null
  - separator: null
  - decoder:   null