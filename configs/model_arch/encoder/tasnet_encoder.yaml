_target_: "src.models.encoder.TasNetEncoder"
num_filters: 256           # number of basis filters (output dimensionality -> expressive)
filter_length_ms: 4.0     # filter length in ms (64 filters at @16 kHz)
stride_ms: ${model_arch.stride_ms}
sample_rate: ${model_arch.sample_rate}
causal: true              # causal convolution