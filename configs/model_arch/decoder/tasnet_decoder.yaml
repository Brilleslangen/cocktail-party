_target_: "src.models.decoder.TasNetDecoder"
num_filters: ${model_arch.feature_dim}            # number of basis filters (output dimensionality -> expressive)
filter_length_ms: ${model_arch.filter_length_ms}  # filter length in ms (64 filters at @16 kHz)
stride_ms: ${model_arch.stride_ms}
sample_rate: ${model_arch.sample_rate}
causal: false                                     # causal convolution