# @package _global_
defaults:
  - /dataset: static_big         # Dataset selection
  - /training: default
  - /wandb: default
  - _self_

group: 0-baselines
model_artifact: none

wandb:
  tags: [evaluation]


hydra:
  mode: MULTIRUN
  sweeper:
    params:
      model_artifact: oracle-mix-offline:latest, oracle-targets-offline:latest, oracle-mix-streaming:latest,
                      oracle-targets-streaming:latest
