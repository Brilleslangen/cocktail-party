# @package _global_
defaults:
  - s-transformer-sym
  - _self_

  - override /model_arch/separator: tcn

# adjustments
name: L-${model_arch.separator.name}-streaming-sym-bidirect

model_arch:
  separator:
    scaler: 2.0

