# @package _global_
defaults:
  - s-transformer-context
  - _self_

  - override /model_arch/separator: mamba

# adjustments
name: S-${model_arch.separator.name}-streaming-context

training:
    params:
        batch_size: 70